#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define vl vector<ll>

int main(){
ios_base::sync_with_stdio(false); cin.tie(0);
  ll n,k;
  cin>>n>>k;
  string tmp;
  string min_pref;
  string min_abs;
  cin>>tmp;
  min_pref=min_abs=tmp;
  for (int i=1;i<n;i++){
    cin>>tmp;
    if (tmp<min_pref){
        if (str)
        min_pref=tmp;
    }
  }
}w