#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin >> t;
    while (-t>=0){
        int n;
        int tmp;
        cin >>n;
        cout << "digit"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "digit"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "add -5"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "add -3"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "add -2"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "add -1"<<endl;
        cout.flush();
        cin >> tmp;
        cout << "add "<<n-1<<endl;
        cout.flush();
        cin >> tmp;
        cout << "!"<<endl;
        cout.flush();
        cin >> tmp;
        
    }
}